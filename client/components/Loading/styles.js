export default theme => ({
  progress: {
    margin: theme.spacing.unit * 2
  },
  loadingWrapper: {
    display: 'flex',
    height: '100%',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center'
  }
});
